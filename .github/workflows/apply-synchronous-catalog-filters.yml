name: Senkron katalog filtre düzeltmesini uygula

on:
  push:
    branches: [agent/browser-flow-regression-audit]
    paths:
      - scripts/apply-synchronous-catalog-filters.mjs
      - .github/workflows/apply-synchronous-catalog-filters.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Depoyu al
        uses: actions/checkout@v6
        with:
          ref: agent/browser-flow-regression-audit
          fetch-depth: 0
      - name: Node.js kur
        uses: actions/setup-node@v6
        with:
          node-version: 22.18.0
          package-manager-cache: false
      - name: Düzeltmeyi uygula
        run: node scripts/apply-synchronous-catalog-filters.mjs
      - name: Kalıcı değişikliği kaydet
        shell: bash
        run: |
          if git diff --quiet -- catalog-filters.jsx; then
            echo "Uygulanacak değişiklik yok."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog-filters.jsx
          git commit -m "fix: katalog filtrelerini güncel state ile senkron çalıştır"
          git pull --rebase origin agent/browser-flow-regression-audit
          git push origin HEAD:agent/browser-flow-regression-audit