name: Son tarayıcı akış düzeltmelerini uygula

on:
  push:
    branches: [agent/browser-flow-regression-audit]
    paths:
      - scripts/apply-final-browser-flow-fixes.mjs
      - .github/workflows/apply-final-browser-flow-fixes.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Depoyu al
        uses: actions/checkout@v6
        with:
          ref: agent/browser-flow-regression-audit
          fetch-depth: 0

      - name: Node.js kur
        uses: actions/setup-node@v6
        with:
          node-version: 22.18.0
          package-manager-cache: false

      - name: Düzeltmeleri uygula
        run: node scripts/apply-final-browser-flow-fixes.mjs

      - name: Uygulama değişikliklerini kaydet
        shell: bash
        run: |
          if git diff --quiet -- app.jsx catalog-filters.jsx; then
            echo "Uygulanacak yeni uygulama değişikliği yok."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.jsx catalog-filters.jsx
          git commit -m "fix: katalog geçişini ve filtre sıfırlamayı tamamla"
          git pull --rebase origin agent/browser-flow-regression-audit
          git push origin HEAD:agent/browser-flow-regression-audit
