name: Tarayıcı akış düzeltmelerini uygula

on:
  push:
    branches: [agent/browser-flow-regression-audit]
    paths:
      - scripts/apply-browser-flow-fixes.mjs
      - .github/workflows/apply-browser-flow-fixes.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Depoyu al
        uses: actions/checkout@v6
        with:
          ref: agent/browser-flow-regression-audit
          fetch-depth: 0

      - name: Node.js kur
        uses: actions/setup-node@v6
        with:
          node-version: 22.18.0
          package-manager-cache: false

      - name: Düzeltmeleri uygula
        run: node scripts/apply-browser-flow-fixes.mjs

      - name: Değişiklikleri kaydet
        shell: bash
        run: |
          if git diff --quiet -- app.jsx; then
            echo "Uygulanacak yeni uygulama değişikliği yok."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.jsx
          git commit -m "fix: tarayıcı akışında çift geçiş ve eski seçimleri düzelt"
          git push origin HEAD:agent/browser-flow-regression-audit
